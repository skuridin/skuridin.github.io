{
  "private": true,
  "scripts": {
    "build_for_client": "NODE_ENV=production webpack -c -p --progress --optimize-occurence-order --optimize-dedupe",
    "build_for_server": "NODE_ENV=production webpack --config ./webpack.server.config.js -c -p --progress --optimize-occurence-order --optimize-dedupe",
    "build": "npm run build_for_client && npm run build_for_server",
    "dev": "webpack-dev-server -d --progress --optimize-occurence-order --optimize-dedupe"
  },
  "devDependencies": {
    "autoprefixer-loader": "^2.0.0",
    "babel-core": "^5.8.22",
    "babel-loader": "^5.3.2",
    "clean-webpack-plugin": "^0.1.3",
    "css-loader": "^0.16.0",
    "extract-text-webpack-plugin": "^0.8.2",
    "file-loader": "^0.8.4",
    "html-webpack-plugin": "^1.6.1",
    "image-webpack-loader": "^1.6.1",
    "imagemin": "^3.2.0",
    "normalize.css": "^3.0.3",
    "null-loader": "^0.1.1",
    "react": "^0.13.3",
    "react-document-title": "^1.0.3",
    "react-router": "^0.13.3",
    "stylus-loader": "^1.2.1",
    "webpack": "^1.11.0",
    "webpack-dev-server": "^1.10.1"
  }
}
