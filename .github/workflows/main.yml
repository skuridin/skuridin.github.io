name: "Deploy to Github pages"

# on:
#   push:
#     branches:
#       - "master"

on: "push:

jobs:
  deploy:
    runs-on: "ubuntu-latest"
    steps:
      - name: "Upload"
        run: |
          git status
        
