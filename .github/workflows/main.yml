name: "Deploy to Github pages"

# on:
#   push:
#     branches:
#       - "master"

on: "push"

jobs:
  deploy:
    name: "Deploying"
    runs-on: "ubuntu-latest"
    steps:
      - name: "Getting status"
        run: ls -alh
        
